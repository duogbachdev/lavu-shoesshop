export interface SupplierModel {
  name: string
  slug: string
  product: string
  category: string[]
  price: number
  contact: string
  isTaking: number
  photoUrl: string
  createdAt: string
  updatedAt: string,
  email: string,
  active: number,
  _id: string
}
